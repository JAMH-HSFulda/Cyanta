using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AudioTest : MonoBehaviour
{
   /*
   //Sound der Munition beim Einsammeln

   //Grafik deaktivieren
   collisionInfo.gameObject.GetComponent<Renderer>().enable = false;
   //Collider deaktivieren
   collisionInfo.gameObject.GetComponent<Collider>().enable = false;
   //Sound für Münze abspielen
   AudioSource audio = collisionInfo.gameObject.GetComponent<AudioSource>();
   audio.Play();

   //Münze zerstören
   //, float --> audio.clip.length;

   */
}
